using System;

namespace TestAssembly
{
    public class TestClass
    {
        public double Pow2(int a)
            => Math.Pow(a, 2);

        public double Pow3(int a)
            => Math.Pow(a, 3);
    }
}
